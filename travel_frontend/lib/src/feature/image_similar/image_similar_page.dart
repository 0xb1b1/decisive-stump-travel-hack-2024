import 'package:flutter/material.dart';

class ImageSimilarPage extends StatelessWidget {
  const ImageSimilarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
